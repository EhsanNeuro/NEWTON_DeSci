import { Bridge } from 'twa-bridge';
import { Version } from 'twa-core';
import { BridgeLike } from '../../types';
/**
 * Represents wrapper around Bridge which provides some extra functionality.
 */
export declare class BridgeScoped implements BridgeLike {
    private readonly bridge;
    private readonly version;
    constructor(bridge: Bridge, version: Version);
    postEvent: typeof this.bridge.postEvent;
    on: <E extends keyof import("twa-bridge").BridgeEventsMap>(event: E, listener: import("twa-core").EventListener<import("twa-bridge").BridgeEventsMap[E]>) => void;
    off: <E extends keyof import("twa-bridge").BridgeEventsMap>(event: E, listener: import("twa-core").EventListener<import("twa-bridge").BridgeEventsMap[E]>) => void;
}
