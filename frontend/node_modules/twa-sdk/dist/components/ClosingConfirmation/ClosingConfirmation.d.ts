import { ClosingConfirmationEventsMap } from './events';
import { BridgeLike } from '../../types';
/**
 * Component responsible for controlling current closing confirmation
 * status.
 */
export declare class ClosingConfirmation {
    private readonly bridge;
    private readonly ee;
    private _isEnabled;
    constructor(bridge: BridgeLike);
    private set isEnabled(value);
    /**
     * Returns true, if the confirmation dialog enabled while the user is trying
     * to close the Web App.
     */
    get isEnabled(): boolean;
    /**
     * Disables the confirmation dialog while the user is trying to close the
     * Web App.
     */
    disable(): void;
    /**
     * Enables the confirmation dialog while the user is trying to close the
     * Web App.
     */
    enable(): void;
    /**
     * Adds new event listener.
     */
    on: <E extends "change">(event: E, listener: import("twa-core").EventListener<ClosingConfirmationEventsMap[E]>) => void;
    /**
     * Removes event listener.
     */
    off: <E extends "change">(event: E, listener: import("twa-core").EventListener<ClosingConfirmationEventsMap[E]>) => void;
}
